{
  "name": "grocerynotes-functions-stub",
  "private": true,
  "engines": { "node": "18" },
  "main": "index.js",
  "dependencies": {
    "firebase-functions": "^4.4.0"
  },
  "scripts": {
    "start": "firebase emulators:start --only functions",
    "serve": "firebase emulators:start --only functions"
  }
}
